<p id="notice"><%= notice %></p>

<h1>Listing Commments</h1>

<table>
  <thead>
    <tr>
      <th>User name</th>
      <th>Body</th>
      <th>Idea</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @commments.each do |commment| %>
      <tr>
        <td><%= commment.user_name %></td>
        <td><%= commment.body %></td>
        <td><%= commment.idea_id %></td>
        <td><%= link_to 'Show', commment %></td>
        <td><%= link_to 'Edit', edit_commment_path(commment) %></td>
        <td><%= link_to 'Destroy', commment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Commment', new_commment_path %>
