<%= form_for(@commment) do |f| %>
  <% if @commment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@commment.errors.count, "error") %> prohibited this commment from being saved:</h2>

      <ul>
      <% @commment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_name %><br>
    <%= f.text_field :user_name %>
  </div>

  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <%= f.hidden_field :idea_id %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
